interface Event {
    // define the properties of the event object.
}

export function handler(event: Event) {
    /**
     * @summary Set up as if we were running this in AWS,
     * though I'll probably change depending on where this
     * is hosted.
     */

    
}